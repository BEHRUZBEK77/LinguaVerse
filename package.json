{
  "name": "linguaverse",
  "version": "2.0.0",
  "description": "AI bilan til o'rganish platformasi",
  "private": true,
  "scripts": {
    "start": "node dev-server.js",
    "dev": "node dev-server.js",
    "check": "node scripts/check-wiring.js && node scripts/check-plan-sync.js"
  },
  "engines": { "node": ">=18" }
}
